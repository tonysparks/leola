// print("x");

var f = def (x) {
    return x + 3;
}
 
var g = def(function, x) {
    return function(x) * function(x);
}
 
println ( g(f, 7) );


var map = new java.util.HashMap();
var x = map.put( "Hello" , 1 );
map.put("World", 2);
var o = map.get("Hello");
println(o);

var test = def(map) {
	map = null;
	println(map);
}

test(map);
println(map);


// var map = new java.util.HashMap();
var keySet = map.keySet();
var it = keySet.iterator();
while( it.hasNext() ) {
	var s = it.next();
	print(s);
}